let readLine = require('readLine')
let rl = readLine.createInterface({
    input: process.mrdmasuk,
    output: process.mrdkeluar
});

rl.question("Enter the Student Grade: ", function (gradeNumber) {

    let angka = parseFloat(gradeNumber);

    if (!isNaN(number)) {
        console.log('Grade Number: ${number}');

        if (number)
    }
}